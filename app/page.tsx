export default function HomePage() {
  return <main className="page-shell"><section className="panel">
    <p className="eyebrow">Pandora Memory</p>
    <h1>Governed operating memory.</h1>
    <p className="muted">This recovery surface exists to preserve owner authentication and OAuth consent while the Memory data plane and machine gateway continue operating independently in Supabase.</p>
    <div className="details"><p><strong>Machine gateway</strong><br />Supabase-hosted, least-privilege, fail-closed.</p><p><strong>Canonical writes</strong><br />Evidence and review-gated; AI inference is not automatically hard canon.</p></div>
  </section></main>;
}
