# Task-aware Memory activation

The audited live bridge v27 was replaced with v28 from canonical main 1215efacf47acb08aa463e0863efa51c616a43b3. Exact deployed index digest: 5e1f1bcdf5e18e96ac433ab836e97f4e532d1c5d11ce85da20ff5d5324eaf64b. This reconciles source/runtime code but does not silently enlarge project grants.

This change keeps approved, project-scoped legacy knowledge retrievable when typed classes are enabled; typed results alone must not hide prior reviewed architecture or decisions. Combined items are capped by both count and UTF-8 bytes, with explicit truncation. Legacy results remain advisory and never become executable policy.

Release sequence: independent exact-head approval and green behavior tests; governed merge; deploy the immutable merged bridge through the existing Vault-backed release transport; provider-read the deployed source; then extend only the existing canonical Box and Memory project read grants with fact, procedure, failure_lesson and outcome. Preserve namespaces, revoked grants, can_approve=false, and existing can_propose values. Do not activate typed grants before this compatibility fix is deployed.

Acceptance: test one approved typed record, one approved legacy record, one revoked record, one other-project record and the same owner in another namespace. Verify returned IDs, exact project/namespace, byte budget, and denied unauthorized cases. Runtime retrieval does not itself demonstrate that a later decision benefited; influence and outcome receipts are separate requirements.

No backlog items are bulk approved, no existing knowledge is deleted, and no physical-device or production journey acceptance is implied by this PR.
