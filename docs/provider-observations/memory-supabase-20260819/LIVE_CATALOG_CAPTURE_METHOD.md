# Pandora Memory live catalog capture method

This directory is a supplemental, read-only production observation for Supabase project `ivmvufhcsezyhczzondn` captured on 2026-08-19.

The files are generated from PostgreSQL catalog queries executed through the governed Supabase Management API endpoint `database/query/read-only`. The provider computed each payload's byte count and SHA-256 before the bytes were versioned. No production DDL, DML, Edge deployment, branch creation, credential rotation, or data mutation was performed.

The authoritative summary and expected content hashes are in `LIVE_CATALOG_MANIFEST.json`. Existing PR #31 remains authoritative for authentic migration-source classification, and PR #34 remains the preserved historical Edge-function recovery dependency. This supplemental evidence does not manufacture missing migration SQL or qualify database rollback.
